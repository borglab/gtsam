name: timeSFMBAL Benchmark Runner
run-name: timeSFMBAL worker ${{ inputs.group_id }} ${{ inputs.runner_id }} ${{ inputs.target_sha }}

on:
  workflow_dispatch:
    inputs:
      group_id:
        description: Unique dispatch group ID used by orchestrator
        required: true
        type: string
      runner_id:
        description: Runner profile ID
        required: true
        type: string
      os_name:
        description: OS label used in cache key
        required: true
        type: string
      arch:
        description: Architecture label used in cache key
        required: true
        type: string
      checkout_ref:
        description: Git ref/SHA to checkout for benchmarking
        required: true
        type: string
      target_sha:
        description: Commit SHA used in cache key
        required: true
        type: string
      orchestrator_run_id:
        description: Orchestrator run ID for cache-busting
        required: true
        type: string

permissions:
  contents: read

jobs:
  benchmark_linux_x64:
    if: inputs.runner_id == 'linux-x64'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target ref
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.checkout_ref }}
          fetch-depth: 0

      - name: Restore BAL dataset archive cache
        id: dataset_cache_restore
        uses: actions/cache/restore@v4
        with:
          path: examples/Data/problem-135-90642-pre.txt.bz2
          key: bal-problem-135-90642-pre-txt-bz2-v1

      - name: Download BAL dataset archive
        if: steps.dataset_cache_restore.outputs.cache-hit != 'true'
        run: |
          curl --fail --location --show-error \
            --output examples/Data/problem-135-90642-pre.txt.bz2 \
            https://grail.cs.washington.edu/projects/bal/data/dubrovnik/problem-135-90642-pre.txt.bz2

      - name: Save BAL dataset archive cache
        if: steps.dataset_cache_restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: examples/Data/problem-135-90642-pre.txt.bz2
          key: bal-problem-135-90642-pre-txt-bz2-v1

      - name: Extract BAL dataset
        run: |
          bzip2 -dc examples/Data/problem-135-90642-pre.txt.bz2 > examples/Data/dubrovnik-135-90642-pre.txt

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libtbb-dev

      # ── TBB=ON ──
      - name: Configure (TBB=ON)
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_FLAGS="-w" \
            -DGTSAM_BUILD_TESTS=OFF \
            -DGTSAM_BUILD_UNSTABLE=ON \
            -DGTSAM_BUILD_EXAMPLES_ALWAYS=OFF \
            -DGTSAM_BUILD_TIMING_ALWAYS=ON \
            -DGTSAM_BUILD_WITH_MARCH_NATIVE=OFF \
            -DGTSAM_WITH_TBB=ON \
            -DGTSAM_USE_BOOST_FEATURES=OFF \
            -DGTSAM_ENABLE_BOOST_SERIALIZATION=OFF

      - name: Build timeSFMBAL (TBB=ON)
        run: cmake --build build --target timeSFMBAL -j2

      - name: Run benchmark (TBB=ON)
        run: ./build/timing/timeSFMBAL --benchmark-action-json benchmark-results-tbbON.json examples/Data/dubrovnik-135-90642-pre.txt

      - name: Prepare cache payload (TBB=ON)
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p benchmark-cache
          cp benchmark-results-tbbON.json "benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbON-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json"

      - name: Save benchmark cache (TBB=ON)
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbON-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json
          key: timeSFMBAL-benchmark-v4-${{ inputs.os_name }}-${{ inputs.arch }}-tbbON-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}

      # ── TBB=OFF ──
      - name: Clean build directory
        run: rm -rf build

      - name: Configure (TBB=OFF)
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_FLAGS="-w" \
            -DGTSAM_BUILD_TESTS=OFF \
            -DGTSAM_BUILD_UNSTABLE=ON \
            -DGTSAM_BUILD_EXAMPLES_ALWAYS=OFF \
            -DGTSAM_BUILD_TIMING_ALWAYS=ON \
            -DGTSAM_BUILD_WITH_MARCH_NATIVE=OFF \
            -DGTSAM_WITH_TBB=OFF \
            -DGTSAM_USE_BOOST_FEATURES=OFF \
            -DGTSAM_ENABLE_BOOST_SERIALIZATION=OFF

      - name: Build timeSFMBAL (TBB=OFF)
        run: cmake --build build --target timeSFMBAL -j2

      - name: Run benchmark (TBB=OFF)
        run: ./build/timing/timeSFMBAL --benchmark-action-json benchmark-results-tbbOFF.json examples/Data/dubrovnik-135-90642-pre.txt

      - name: Prepare cache payload (TBB=OFF)
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p benchmark-cache
          cp benchmark-results-tbbOFF.json "benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbOFF-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json"

      - name: Save benchmark cache (TBB=OFF)
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbOFF-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json
          key: timeSFMBAL-benchmark-v4-${{ inputs.os_name }}-${{ inputs.arch }}-tbbOFF-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}

  benchmark_linux_arm64:
    if: inputs.runner_id == 'linux-arm64'
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout target ref
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.checkout_ref }}
          fetch-depth: 0

      - name: Restore BAL dataset archive cache
        id: dataset_cache_restore
        uses: actions/cache/restore@v4
        with:
          path: examples/Data/problem-135-90642-pre.txt.bz2
          key: bal-problem-135-90642-pre-txt-bz2-v1

      - name: Download BAL dataset archive
        if: steps.dataset_cache_restore.outputs.cache-hit != 'true'
        run: |
          curl --fail --location --show-error \
            --output examples/Data/problem-135-90642-pre.txt.bz2 \
            https://grail.cs.washington.edu/projects/bal/data/dubrovnik/problem-135-90642-pre.txt.bz2

      - name: Save BAL dataset archive cache
        if: steps.dataset_cache_restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: examples/Data/problem-135-90642-pre.txt.bz2
          key: bal-problem-135-90642-pre-txt-bz2-v1

      - name: Extract BAL dataset
        run: |
          bzip2 -dc examples/Data/problem-135-90642-pre.txt.bz2 > examples/Data/dubrovnik-135-90642-pre.txt

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libtbb-dev

      # ── TBB=ON ──
      - name: Configure (TBB=ON)
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_FLAGS="-w" \
            -DGTSAM_BUILD_TESTS=OFF \
            -DGTSAM_BUILD_UNSTABLE=ON \
            -DGTSAM_BUILD_EXAMPLES_ALWAYS=OFF \
            -DGTSAM_BUILD_TIMING_ALWAYS=ON \
            -DGTSAM_BUILD_WITH_MARCH_NATIVE=OFF \
            -DGTSAM_WITH_TBB=ON \
            -DGTSAM_USE_BOOST_FEATURES=OFF \
            -DGTSAM_ENABLE_BOOST_SERIALIZATION=OFF

      - name: Build timeSFMBAL (TBB=ON)
        run: cmake --build build --target timeSFMBAL -j2

      - name: Run benchmark (TBB=ON)
        run: ./build/timing/timeSFMBAL --benchmark-action-json benchmark-results-tbbON.json examples/Data/dubrovnik-135-90642-pre.txt

      - name: Prepare cache payload (TBB=ON)
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p benchmark-cache
          cp benchmark-results-tbbON.json "benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbON-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json"

      - name: Save benchmark cache (TBB=ON)
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbON-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json
          key: timeSFMBAL-benchmark-v4-${{ inputs.os_name }}-${{ inputs.arch }}-tbbON-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}

      # ── TBB=OFF ──
      - name: Clean build directory
        run: rm -rf build

      - name: Configure (TBB=OFF)
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_FLAGS="-w" \
            -DGTSAM_BUILD_TESTS=OFF \
            -DGTSAM_BUILD_UNSTABLE=ON \
            -DGTSAM_BUILD_EXAMPLES_ALWAYS=OFF \
            -DGTSAM_BUILD_TIMING_ALWAYS=ON \
            -DGTSAM_BUILD_WITH_MARCH_NATIVE=OFF \
            -DGTSAM_WITH_TBB=OFF \
            -DGTSAM_USE_BOOST_FEATURES=OFF \
            -DGTSAM_ENABLE_BOOST_SERIALIZATION=OFF

      - name: Build timeSFMBAL (TBB=OFF)
        run: cmake --build build --target timeSFMBAL -j2

      - name: Run benchmark (TBB=OFF)
        run: ./build/timing/timeSFMBAL --benchmark-action-json benchmark-results-tbbOFF.json examples/Data/dubrovnik-135-90642-pre.txt

      - name: Prepare cache payload (TBB=OFF)
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p benchmark-cache
          cp benchmark-results-tbbOFF.json "benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbOFF-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json"

      - name: Save benchmark cache (TBB=OFF)
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbOFF-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json
          key: timeSFMBAL-benchmark-v4-${{ inputs.os_name }}-${{ inputs.arch }}-tbbOFF-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}

  benchmark_macos_arm64:
    if: inputs.runner_id == 'macos-arm64'
    runs-on: macos-15
    steps:
      - name: Checkout target ref
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.checkout_ref }}
          fetch-depth: 0

      - name: Restore BAL dataset archive cache
        id: dataset_cache_restore
        uses: actions/cache/restore@v4
        with:
          path: examples/Data/problem-135-90642-pre.txt.bz2
          key: bal-problem-135-90642-pre-txt-bz2-v1

      - name: Download BAL dataset archive
        if: steps.dataset_cache_restore.outputs.cache-hit != 'true'
        run: |
          curl --fail --location --show-error \
            --output examples/Data/problem-135-90642-pre.txt.bz2 \
            https://grail.cs.washington.edu/projects/bal/data/dubrovnik/problem-135-90642-pre.txt.bz2

      - name: Save BAL dataset archive cache
        if: steps.dataset_cache_restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: examples/Data/problem-135-90642-pre.txt.bz2
          key: bal-problem-135-90642-pre-txt-bz2-v1

      - name: Extract BAL dataset
        run: |
          bzip2 -dc examples/Data/problem-135-90642-pre.txt.bz2 > examples/Data/dubrovnik-135-90642-pre.txt

      - name: Install dependencies
        run: brew install tbb || true

      # ── TBB=ON ──
      - name: Configure (TBB=ON)
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_FLAGS="-w" \
            -DGTSAM_BUILD_TESTS=OFF \
            -DGTSAM_BUILD_UNSTABLE=ON \
            -DGTSAM_BUILD_EXAMPLES_ALWAYS=OFF \
            -DGTSAM_BUILD_TIMING_ALWAYS=ON \
            -DGTSAM_BUILD_WITH_MARCH_NATIVE=OFF \
            -DGTSAM_WITH_TBB=ON \
            -DGTSAM_USE_BOOST_FEATURES=OFF \
            -DGTSAM_ENABLE_BOOST_SERIALIZATION=OFF

      - name: Build timeSFMBAL (TBB=ON)
        run: cmake --build build --target timeSFMBAL -j2

      - name: Run benchmark (TBB=ON)
        run: ./build/timing/timeSFMBAL --benchmark-action-json benchmark-results-tbbON.json examples/Data/dubrovnik-135-90642-pre.txt

      - name: Prepare cache payload (TBB=ON)
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p benchmark-cache
          cp benchmark-results-tbbON.json "benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbON-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json"

      - name: Save benchmark cache (TBB=ON)
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbON-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json
          key: timeSFMBAL-benchmark-v4-${{ inputs.os_name }}-${{ inputs.arch }}-tbbON-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}

      # ── TBB=OFF ──
      - name: Clean build directory
        run: rm -rf build

      - name: Configure (TBB=OFF)
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_FLAGS="-w" \
            -DGTSAM_BUILD_TESTS=OFF \
            -DGTSAM_BUILD_UNSTABLE=ON \
            -DGTSAM_BUILD_EXAMPLES_ALWAYS=OFF \
            -DGTSAM_BUILD_TIMING_ALWAYS=ON \
            -DGTSAM_BUILD_WITH_MARCH_NATIVE=OFF \
            -DGTSAM_WITH_TBB=OFF \
            -DGTSAM_USE_BOOST_FEATURES=OFF \
            -DGTSAM_ENABLE_BOOST_SERIALIZATION=OFF

      - name: Build timeSFMBAL (TBB=OFF)
        run: cmake --build build --target timeSFMBAL -j2

      - name: Run benchmark (TBB=OFF)
        run: ./build/timing/timeSFMBAL --benchmark-action-json benchmark-results-tbbOFF.json examples/Data/dubrovnik-135-90642-pre.txt

      - name: Prepare cache payload (TBB=OFF)
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p benchmark-cache
          cp benchmark-results-tbbOFF.json "benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbOFF-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json"

      - name: Save benchmark cache (TBB=OFF)
        uses: actions/cache/save@v4
        continue-on-error: true
        with:
          path: benchmark-cache/${{ inputs.os_name }}-${{ inputs.arch }}-tbbOFF-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}.json
          key: timeSFMBAL-benchmark-v4-${{ inputs.os_name }}-${{ inputs.arch }}-tbbOFF-${{ inputs.target_sha }}-${{ inputs.orchestrator_run_id }}
